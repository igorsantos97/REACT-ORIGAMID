const grupoA = 100;
const grupoB = 200;

// // Com JSX só sera possível usar Expressões e não IF e ELSE
// const vencedor = grupoA > grupoB ? 'Grupo A Venceu' : 'Grupo B Venceu';
// console.log(vencedor);

const active = true;
const button = active && 'Botão está ativo'; // Botão está ativo

console.log(button);
